---
phase: 03-interactive-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/Services/ReportService.php, app/Controllers/ReportController.php, app/Views/reports/dashboard.php, app/Config/Routes.php]
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "Dashboard shows monthly trend chart for selected ward."
    - "Dashboard shows cross-ward comparison chart for selected month/year."
    - "User can filter dashboard data by ward, month, and year."
  artifacts:
    - path: "app/Views/reports/dashboard.php"
      provides: "Interactive dashboard UI with Chart.js"
    - path: "app/Controllers/ReportController.php"
      provides: "Dashboard endpoints for page and AJAX data"
    - path: "app/Services/ReportService.php"
      provides: "Trend and comparison datasets"
  key_links:
    - from: "app/Controllers/ReportController.php"
      to: "app/Services/ReportService.php"
      via: "dashboardData()"
      pattern: "getYearlyTrend|getWardComparison"
---

<objective>
Implement the first interactive dashboard slice for management insights.

Purpose: To provide visual trend monitoring and cross-ward comparison using existing monthly-reporting data.
Output: A dashboard page with Chart.js charts and ward/month/year filters.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard Data API</name>
  <files>app/Services/ReportService.php, app/Controllers/ReportController.php</files>
  <action>
    - Add service methods for yearly trend and ward comparison datasets.
    - Add controller endpoint returning structured JSON for dashboard charts.
  </action>
  <verify>
    <automated>php -l app/Services/ReportService.php && php -l app/Controllers/ReportController.php</automated>
  </verify>
  <done>Dashboard data endpoint returns trend and comparison datasets.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard UI and Routes</name>
  <files>app/Views/reports/dashboard.php, app/Config/Routes.php</files>
  <action>
    - Create dashboard view with ward/month/year filter form.
    - Render trend and comparison charts using Chart.js.
    - Register dashboard routes protected with report-view permission.
  </action>
  <verify>
    <automated>php spark routes</automated>
  </verify>
  <done>Authorized users can open dashboard page and load interactive charts.</done>
</task>

</tasks>
