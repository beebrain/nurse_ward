---
phase: 01-foundation-daily-data-entry
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-03]
files_modified: [app/Models/CensusModel.php, app/Controllers/CensusController.php, app/Views/census/create.php]
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, UI-01]
must_haves:
  truths:
    - "User can select a ward and a date for entry"
    - "User can enter patient counts for Morning, Afternoon, and Night shifts"
    - "UI is usable on mobile screens"
  artifacts:
    - path: "app/Database/Migrations/CreateCensusTables.php"
      provides: "Transactional tables for census and ADT"
    - path: "app/Views/census/create.php"
      provides: "The responsive data entry form"
  key_links:
    - from: "app/Views/census/create.php"
      to: "app/Controllers/CensusController.php"
      via: "form POST (fallback) and AJAX handlers"
---

<objective>
Establish the transactional foundation for daily patient census recording.

Purpose: To allow nurses to record daily patient counts across different shifts.
Output: A transactional database schema and a responsive multi-shift data entry interface.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-daily-data-entry/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation-daily-data-entry/01-01-SUMMARY.md
@.planning/phases/01-foundation-daily-data-entry/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Census Database Schema and Transactional Tables</name>
  <files>app/Models/CensusModel.php, app/Database/Migrations/CreateCensusTables.php</files>
  <action>
    - Migration for `daily_census`: `id`, `ward_id`, `record_date`, `shift` (M, A, N), `admissions`, `discharges`, `transfers_in`, `transfers_out`, `deaths`, `total_remaining`.
    - Create `CensusModel` with composite unique index on `ward_id`, `record_date`, and `shift`.
    - Implementation should allow separate entries for each shift per day per ward (per 01-CONTEXT.md).
  </action>
  <verify>
    <automated>php spark migrate:status</automated>
  </verify>
  <done>The `daily_census` table exists with shift-based granularity.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive Shift-based Entry Form</name>
  <files>app/Controllers/CensusController.php, app/Views/census/create.php</files>
  <action>
    - Build the entry form using Bootstrap 5 with mobile-first layout (UI-01).
    - Features: Date picker, Ward selection (filtered by active), Shift selection.
    - Fields: Admissions, Discharges, Transfers (In/Out), Deaths, Remaining.
    - Implementation: Form fields should have intuitive IDs/Classes for future AJAX interaction.
  </action>
  <verify>
    <automated>php spark routes | grep "census/new"</automated>
  </verify>
  <done>Nurses can access a mobile-friendly form to record shift-based census data.</done>
</task>

</tasks>

<verification>
Access the census entry form on a mobile device (or via dev tools). Select a ward, date, and shift. Fill in some values and submit. Verify records are created in the `daily_census` table.
</verification>

<success_criteria>
- Nurses can select from active wards.
- Census data is stored per shift.
- UI is responsive and touch-friendly on mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-daily-data-entry/01-04-SUMMARY.md`
</output>
