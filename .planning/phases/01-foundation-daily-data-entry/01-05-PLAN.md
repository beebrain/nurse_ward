---
phase: 01-foundation-daily-data-entry
plan: 05
type: execute
wave: 3
depends_on: [01-04]
files_modified: [public/js/census_entry.js, app/Controllers/CensusController.php, app/Views/census/create.php]
autonomous: true
requirements: [DATA-04, DATA-05, UI-03]
must_haves:
  truths:
    - "System auto-saves data as user types (field change)"
    - "User receives visual feedback for auto-save status"
    - "Invalid input (e.g., negative numbers) is prevented"
  artifacts:
    - path: "public/js/census_entry.js"
      provides: "The AJAX auto-save logic"
    - path: "app/Controllers/CensusController.php"
      provides: "AJAX handling endpoint"
  key_links:
    - from: "public/js/census_entry.js"
      to: "/census/autosave"
      via: "jQuery AJAX POST"
---

<objective>
Enhance the daily census entry experience with AJAX auto-save and robust input validation.

Purpose: To prevent data loss during interruptions and ensure the integrity of the collected statistical data.
Output: A dynamic, AJAX-powered entry form with real-time validation.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-daily-data-entry/01-CONTEXT.md
@.planning/phases/01-foundation-daily-data-entry/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AJAX Auto-save Implementation</name>
  <files>public/js/census_entry.js, app/Controllers/CensusController.php</files>
  <action>
    - Build an AJAX handler in `CensusController` to handle partial updates for shift-based records.
    - Create `public/js/census_entry.js` using jQuery to detect `change` events on input fields.
    - Implement a debounced AJAX POST to save the current form state to the database (DATA-04).
    - Provide a visual "Saving..." and "Saved" indicator in the UI (UI-03).
  </action>
  <verify>
    <automated>grep -E "$.ajax" public/js/census_entry.js</automated>
  </verify>
  <done>Changes to census fields are automatically persisted to the database via AJAX.</done>
</task>

<task type="auto">
  <name>Task 2: Input Validation and Integrity Checks</name>
  <files>app/Controllers/CensusController.php, public/js/census_entry.js</files>
  <action>
    - Implement backend validation in the AJAX controller: `required`, `numeric`, `greater_than_equal_to[0]` (DATA-05).
    - Implement frontend validation via HTML5 `min="0"` and jQuery input masks or listeners to prevent negative entries.
    - Add error handling to the AJAX response to display validation errors without page reload.
  </action>
  <verify>
    <automated>grep -E "greater_than_equal_to" app/Controllers/CensusController.php</automated>
  </verify>
  <done>Invalid inputs are blocked and the user is notified without interrupting their workflow.</done>
</task>

</tasks>

<verification>
Access the census entry form. Enter a positive number in a field, wait for the "Saved" indicator, and refresh the page. Confirm the data persists. Try entering a negative number or non-numeric value and verify the system blocks it and displays an error.
</verification>

<success_criteria>
- Data is saved automatically on field change.
- No negative numbers allowed.
- Error messages are displayed without page refreshes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-daily-data-entry/01-05-SUMMARY.md`
</output>
