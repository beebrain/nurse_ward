---
phase: 01-foundation-daily-data-entry
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [app/Models/UserModel.php, app/Controllers/Admin/UserController.php, app/Views/admin/users/index.php]
autonomous: true
requirements: [AUTH-02]
---

<objective>
Implement the user approval workflow and management dashboard for Super Admins.

Purpose: To ensure only approved users can access system features and allow admins to manage accounts.
Output: An administrative interface for user list management and account approval.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-daily-data-entry/01-CONTEXT.md
@.planning/phases/01-foundation-daily-data-entry/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom Registration and Approval Logic</name>
  <files>app/Models/UserModel.php, app/Database/Migrations/AddStatusToUsers.php</files>
  <action>
    - Create a migration to add an `approval_status` column to the `users` table (enum: 'pending', 'approved', 'deactivated').
    - Override Shield's user registration flow to set new users to 'pending' by default.
    - Implement a Shield filter to check `approval_status` and redirect 'pending' or 'deactivated' users to a restricted page.
  </action>
  <verify>
    <automated>php spark migrate:status</automated>
  </verify>
  <done>New registrations default to 'pending' and are restricted from the main dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Admin User Management UI</name>
  <files>app/Controllers/Admin/UserController.php, app/Views/admin/users/index.php</files>
  <action>
    - Create a Super Admin controller to list all users.
    - Implement search and filter by role or status.
    - Add toggle actions for "Approve" and "Deactivate" accounts (per 01-CONTEXT.md).
    - Use AdminLTE 3 / Bootstrap 5 for consistent UI.
  </action>
  <verify>
    <automated>php spark routes | grep "admin/users"</automated>
  </verify>
  <done>Super Admin can view, search, approve, and deactivate users from a central dashboard.</done>
</task>

</tasks>

<verification>
Log in as a new user (pending status) and confirm access is denied. Log in as Super Admin and approve the user, then confirm the user can now access the dashboard.
</verification>

<success_criteria>
- Users cannot access features until approved.
- Super Admin has full CRUD on user accounts.
- Account deactivation preserves audit trails without deleting records.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-daily-data-entry/01-02-SUMMARY.md`
</output>
