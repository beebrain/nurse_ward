---
phase: 01-foundation-daily-data-entry
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [app/Models/WardModel.php, app/Controllers/Admin/WardController.php, app/Views/admin/wards/index.php]
autonomous: true
requirements: [DEPT-01, DEPT-02, DEPT-03]
---

<objective>
Implement the administrative module for ward/department management.

Purpose: To allow dynamic creation and configuration of wards, which serve as the context for daily data entry.
Output: A ward CRUD management dashboard for Super Admins.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ward Database Schema and Model</name>
  <files>app/Models/WardModel.php, app/Database/Migrations/CreateWardsTable.php</files>
  <action>
    - Create a migration for the `wards` table: `id`, `name`, `total_beds`, `is_active`, `created_at`, `updated_at`.
    - Create the `WardModel` using CI4 Query Builder.
    - Set up soft-delete and timestamp support.
  </action>
  <verify>
    <automated>php spark migrate:status</automated>
  </verify>
  <done>The `wards` table exists and the model is configured.</done>
</task>

<task type="auto">
  <name>Task 2: Admin Ward CRUD Interface</name>
  <files>app/Controllers/Admin/WardController.php, app/Views/admin/wards/index.php, app/Views/admin/wards/edit.php</files>
  <action>
    - Build the `WardController` with index, create, store, edit, update, and delete actions.
    - Create responsive views using Bootstrap 5.
    - Ensure 'is_active' toggle works to deactivate departments (DEPT-02).
  </action>
  <verify>
    <automated>php spark routes | grep "admin/wards"</automated>
  </verify>
  <done>Super Admin can dynamically create, edit, and deactivate wards.</done>
</task>

</tasks>

<verification>
Log in as Super Admin and create a new ward. Verify it appears in the list. Edit the ward and verify changes persist. Deactivate the ward and confirm its status changes.
</verification>

<success_criteria>
- Super Admin can manage all ward master data.
- Ward metadata (beds, status) is stored correctly.
- Deactivated wards are marked as such (no hard deletion).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-daily-data-entry/01-03-SUMMARY.md`
</output>
