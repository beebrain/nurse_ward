---
phase: 02-monthly-reporting-metrics
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [app/Controllers/ReportController.php, app/Views/reports/monthly_summary.php, app/Config/Routes.php]
autonomous: true
requirements: [REPT-01, REPT-03]
must_haves:
  truths:
    - "User can view a tabular summary for a specific ward and month."
    - "The summary layout matches the hospital template for data presentation."
  artifacts:
    - path: "app/Controllers/ReportController.php"
      provides: "Reporting controller endpoints"
    - path: "app/Views/reports/monthly_summary.php"
      provides: "Monthly report viewer UI"
  key_links:
    - from: "app/Controllers/ReportController.php"
      to: "ReportService.php"
      via: "dependency injection or instantiation"
      pattern: "ReportService"
---

<objective>
Create the web-based report viewer for monthly summaries.

Purpose: To allow management to view monthly patient counts and metrics in the application.
Output: A monthly summary dashboard with ward and date filters.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-monthly-reporting-metrics/01-PLAN.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportController and Routing</name>
  <files>app/Controllers/ReportController.php, app/Config/Routes.php</files>
  <action>
    - Create `app/Controllers/ReportController.php`.
    - Inject `ReportService` into the controller.
    - Implement `monthly()` method for displaying the report viewer and processing filter parameters.
    - Register routes for `/reports/monthly` and `/reports/get_data`.
  </action>
  <verify>
    <automated>php spark routes | grep -E "/reports/monthly|/reports/get_data"</automated>
  </verify>
  <done>Routing and controller setup complete.</done>
</task>

<task type="auto">
  <name>Task 2: Monthly Summary Dashboard UI</name>
  <files>app/Views/reports/monthly_summary.php</files>
  <action>
    - Create `app/Views/reports/monthly_summary.php`.
    - Implement filters for Ward, Month, and Year.
    - Build the summary table with static headers matching the hospital Excel reference.
    - Fetch and display the aggregated data using ReportService results.
  </action>
  <verify>
    <automated>ls app/Views/reports/monthly_summary.php && php -l app/Controllers/ReportController.php</automated>
  </verify>
  <done>Users can view accurate monthly reports in the browser.</done>
</task>

</tasks>

<verification>
Log in as Manager. Navigate to Monthly Reports. Select a Ward, Month, and Year. Verify the displayed table matches expected aggregated daily data.
</verification>

<success_criteria>
- Report data is filtered correctly by user selection.
- Table layout is responsive and professional.
- Data displays matching aggregated values from ReportService.
</success_criteria>

<output>
After completion, create `.planning/phases/02-monthly-reporting-metrics/02-SUMMARY.md`
</output>
