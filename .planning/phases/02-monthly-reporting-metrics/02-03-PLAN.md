---
phase: 02-monthly-reporting-metrics
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [app/Controllers/ReportController.php, composer.json]
autonomous: true
requirements: [EXCL-01]
must_haves:
  truths:
    - "System generates an .xlsx file that opens without errors."
    - "Excel file data matches the web-based report view."
  artifacts:
    - path: "vendor/phpoffice/phpspreadsheet"
      provides: "Excel generation library"
    - path: "app/Controllers/ReportController.php"
      provides: "Excel download endpoint"
  key_links:
    - from: "app/Controllers/ReportController.php"
      to: "PhpSpreadsheet"
      via: "instantiation"
      pattern: "PhpOffice\\\\PhpSpreadsheet"
---

<objective>
Implement Excel export functionality for monthly reports.

Purpose: To allow management to download official monthly statistics in .xlsx format matching the existing hospital template.
Output: An "Export to Excel" feature using PhpSpreadsheet.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-monthly-reporting-metrics/02-PLAN.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install PhpSpreadsheet</name>
  <files>composer.json</files>
  <action>
    - Install `phpoffice/phpspreadsheet` via composer if not already present.
    - If restricted from running composer directly, verify if it can be added to composer.json and wait for vendor sync.
  </action>
  <verify>
    <automated>composer show phpoffice/phpspreadsheet</automated>
  </verify>
  <done>PhpSpreadsheet is ready for use.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Excel Export Logic</name>
  <files>app/Controllers/ReportController.php</files>
  <action>
    - Implement `exportExcel(int $wardId, int $month, int $year)` in `ReportController`.
    - Map `ReportService` data to Spreadsheet cells.
    - Set response headers for `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`.
  </action>
  <verify>
    <automated>php spark routes | grep -i export</automated>
  </verify>
  <done>Management can download Excel reports.</done>
</task>

</tasks>

<verification>
Generate a monthly report. Click 'Export to Excel'. Open the downloaded file and confirm data and formatting match the system's web view.
</verification>

<success_criteria>
- Excel file downloads with correct headers and data.
- Calculations in Excel file match the ReportService output.
- Response headers are correctly set for file download.
</success_criteria>

<output>
After completion, create `.planning/phases/02-monthly-reporting-metrics/03-SUMMARY.md`
</output>
