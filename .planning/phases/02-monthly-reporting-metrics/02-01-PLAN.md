---
phase: 02-monthly-reporting-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/Services/ReportService.php, tests/unit/ReportServiceTest.php]
autonomous: true
requirements: [REPT-02]
must_haves:
  truths:
    - "System can accurately sum daily admissions, discharges, and transfers for a given month/ward."
    - "Productivity calculations match the hospital's mathematical formulas."
  artifacts:
    - path: "app/Services/ReportService.php"
      provides: "Aggregation and productivity logic"
    - path: "tests/unit/ReportServiceTest.php"
      provides: "Verification of metrics math"
  key_links:
    - from: "app/Services/ReportService.php"
      to: "CensusModel (database)"
      via: "find/where queries"
      pattern: "CensusModel"
---

<objective>
Implement the core business logic for aggregating daily census data and calculating productivity metrics.

Purpose: To provide accurate mathematical calculations for monthly reporting.
Output: A ReportService that handles data aggregation and productivity logic.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-monthly-reporting-metrics/RESEARCH.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReportService Aggregation Logic</name>
  <files>app/Services/ReportService.php</files>
  <action>
    - Create `app/Services/ReportService.php`.
    - Implement `getMonthlyData(int $wardId, int $month, int $year)` to aggregate `daily_census` records.
    - Logic should sum admissions, discharges, deaths, etc., and calculate "Total Patient Days".
    - Use WardModel to get bed count for productivity math.
  </action>
  <verify>
    <automated>ls app/Services/ReportService.php && php -l app/Services/ReportService.php</automated>
  </verify>
  <done>Service can aggregate data by ward and month.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Productivity & Metrics Math</name>
  <files>app/Services/ReportService.php, tests/unit/ReportServiceTest.php</files>
  <action>
    - Implement productivity calculation: (Total Patient Days / (Ward Beds * Days in Month)) * 100.
    - Write unit tests in `tests/unit/ReportServiceTest.php` to verify formulas with mock data.
    - Ensure edge cases (e.g., zero beds, empty month) are handled without crashes.
  </action>
  <verify>
    <automated>php spark test tests/unit/ReportServiceTest.php</automated>
  </verify>
  <done>All mathematical formulas are verified by unit tests.</done>
</task>

</tasks>

<verification>
Run unit tests for ReportService and ensure all aggregations match expected values for mock data.
</verification>

<success_criteria>
- Monthly aggregation sums all daily metrics correctly.
- Productivity percentage is calculated accurately.
- Service is decoupled from controllers and is testable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-monthly-reporting-metrics/01-SUMMARY.md`
</output>
